From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: alina sireneva <alina@tei.pet>
Date: Mon, 31 Aug 2026 03:54:04 +0400
Subject: temporary patch with extra debug logs for stale reactions

---
 .../messenger/MessagesController.java         | 27 +++++++++++++++++++
 .../telegram/messenger/MessagesStorage.java   | 12 +++++++++
 .../telegram/ui/Cells/ChatMessageCell.java    |  2 ++
 .../java/org/telegram/ui/ChatActivity.java    | 20 ++++++++++++++
 .../Reactions/ReactionsLayoutInBubble.java    |  2 +-
 5 files changed, 62 insertions(+), 1 deletion(-)

diff --git a/TMessagesProj/src/main/java/org/telegram/messenger/MessagesController.java b/TMessagesProj/src/main/java/org/telegram/messenger/MessagesController.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/messenger/MessagesController.java
+++ b/TMessagesProj/src/main/java/org/telegram/messenger/MessagesController.java
@@ -810,6 +810,9 @@ public class MessagesController extends BaseController implements NotificationCe
                     if (isVotes) {
                         getMessagesStorage().markMessagePollVotesAsRead(dialogId, topicId, messageId);
                     }
+                    if (xie.fa.gram.helpers.chat.ReactionDebugHelper.isEnabled()) {
+                        FileLog.d("InuRx next mention local did=" + dialogId + " topic=" + topicId + " isReactions=" + isReactions + " mid=" + messageId);
+                    }
                     int finalMessageId = messageId;
                     AndroidUtilities.runOnUIThread(() -> callback.accept(finalMessageId));
                 }
@@ -842,6 +845,9 @@ public class MessagesController extends BaseController implements NotificationCe
                     if (error == null && res != null && res.messages != null && !res.messages.isEmpty()) {
                         messageId = res.messages.get(0).id;
                     }
+                    if (xie.fa.gram.helpers.chat.ReactionDebugHelper.isEnabled()) {
+                        FileLog.d("InuRx next mention server did=" + dialogId + " topic=" + topicId + " isReactions=" + isReactions + " addOffset=" + (count - 1) + " expected=" + expectedCount + " current=" + inu_getUnreadCount(dialogId, topicId, isReactions) + " serverCount=" + (res == null ? -1 : res.count) + " serverSize=" + (res == null || res.messages == null ? -1 : res.messages.size()) + " mid=" + messageId + " error=" + error);
+                    }
                     if (error == null && res != null && res.messages != null && canApplyServerCount) {
                         if (messageId != 0) {
                             int serverCount = Math.max(res.count, res.messages.size());
@@ -14263,6 +14269,9 @@ public class MessagesController extends BaseController implements NotificationCe
                             }
                         }
                         if (currentDialog.unread_reactions_count != value.unread_reactions_count) {
+                            if (xie.fa.gram.helpers.chat.ReactionDebugHelper.isEnabled()) {
+                                FileLog.d("InuRx dialogs update did=" + currentDialog.id + " " + currentDialog.unread_reactions_count + " -> " + value.unread_reactions_count + " fromCache=" + fromCache);
+                            }
                             currentDialog.unread_reactions_count = value.unread_reactions_count;
                             getNotificationCenter().postNotificationName(NotificationCenter.dialogsUnreadReactionsCounterChanged, currentDialog.id, 0L, currentDialog.unread_reactions_count, null);
                         }
@@ -21337,6 +21346,9 @@ public class MessagesController extends BaseController implements NotificationCe
     public void inu_onMessageUnreadContentRead(long dialogId, long topicId, int messageId, boolean isReactions) {
         AndroidUtilities.runOnUIThread(() -> {
             int current = inu_getUnreadCount(dialogId, topicId, isReactions);
+            if (xie.fa.gram.helpers.chat.ReactionDebugHelper.isEnabled()) {
+                FileLog.d("InuRx content read did=" + dialogId + " topic=" + topicId + " mid=" + messageId + " isReactions=" + isReactions + " current=" + current);
+            }
             if (current == 1) {
                 if (isReactions) {
                     markReactionsAsRead(dialogId, topicId);
@@ -21387,6 +21399,9 @@ public class MessagesController extends BaseController implements NotificationCe
     }
 
     private void checkUnreadReactionsInternal(long dialogId, long topicId, SparseBooleanArray unreadReactions, boolean isReactions) {
+        if (xie.fa.gram.helpers.chat.ReactionDebugHelper.isEnabled()) {
+            FileLog.d("InuRx check did=" + dialogId + " topic=" + topicId + " isReactions=" + isReactions + " incoming=" + unreadReactions + " from " + xie.fa.gram.helpers.chat.ReactionDebugHelper.caller());
+        }
         final String tableMentionsForDialogs = isReactions ? "reaction_mentions" : "poll_votes_mentions";
         final String tableMentionsForTopics = isReactions ? "reaction_mentions_topics" : "poll_votes_mentions_topics";
         final boolean isVotes = !isReactions;
@@ -21461,6 +21476,9 @@ public class MessagesController extends BaseController implements NotificationCe
                     e.printStackTrace();
                 }
             }
+            if (xie.fa.gram.helpers.chat.ReactionDebugHelper.isEnabled()) {
+                FileLog.d("InuRx check result did=" + dialogId + " topic=" + topicId + " isReactions=" + isReactions + " knownRows=" + reactionsMentionsMessageIds + " needReload=" + needReload + " changed=" + changed + " delta=" + newUnreadCount + " unreadIds=" + newUnreadMessages);
+            }
             if (isReactions) {
                 checkUnreadReactionsInternal2(dialogId, topicId, needReload, changed, newUnreadCount, newUnreadMessages);
             }
@@ -21487,6 +21505,9 @@ public class MessagesController extends BaseController implements NotificationCe
                                 getMessagesStorage().updateDialogUnreadReactions(dialogId, 0, count, false);
                                 return;
                             }
+                            if (xie.fa.gram.helpers.chat.ReactionDebugHelper.isEnabled()) {
+                                FileLog.d("InuRx reload applied did=" + dialogId + " local=" + dialog.unread_reactions_count + " server=" + count);
+                            }
                             dialog.unread_reactions_count = count;
                             getMessagesStorage().updateUnreadReactionsCount(dialogId, topicId, count);
                             getNotificationCenter().postNotificationName(NotificationCenter.dialogsUnreadReactionsCounterChanged, dialogId, topicId, count, newUnreadMessages);
@@ -21533,6 +21554,9 @@ public class MessagesController extends BaseController implements NotificationCe
                         getMessagesStorage().updateDialogUnreadReactions(dialogId, 0, finalNewUnreadCount, true);
                         return;
                     }
+                    if (xie.fa.gram.helpers.chat.ReactionDebugHelper.isEnabled()) {
+                        FileLog.d("InuRx counter delta did=" + dialogId + " " + dialog.unread_reactions_count + " -> " + (dialog.unread_reactions_count + finalNewUnreadCount));
+                    }
                     dialog.unread_reactions_count += finalNewUnreadCount;
                     if (dialog.unread_reactions_count < 0) {
                         dialog.unread_reactions_count = 0;
@@ -21713,6 +21737,9 @@ public class MessagesController extends BaseController implements NotificationCe
     }
 
     public void markReactionsAsRead(long dialogId, long topicId) {
+        if (xie.fa.gram.helpers.chat.ReactionDebugHelper.isEnabled()) {
+            FileLog.d("InuRx mark all read did=" + dialogId + " topic=" + topicId + " current=" + inu_getUnreadCount(dialogId, topicId, true));
+        }
         if (topicId == 0) {
             TLRPC.Dialog dialog = dialogs_dict.get(dialogId);
             if (dialog != null) {
diff --git a/TMessagesProj/src/main/java/org/telegram/messenger/MessagesStorage.java b/TMessagesProj/src/main/java/org/telegram/messenger/MessagesStorage.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/messenger/MessagesStorage.java
+++ b/TMessagesProj/src/main/java/org/telegram/messenger/MessagesStorage.java
@@ -17106,6 +17106,9 @@ public class MessagesStorage extends BaseController {
                         }
                         dialog.folder_id = cursor.intValue(17);
                         dialog.unread_reactions_count = cursor.intValue(19);
+                        if (dialog.unread_reactions_count != 0 && xie.fa.gram.helpers.chat.ReactionDebugHelper.isEnabled()) {
+                            FileLog.d("InuRx db loaded did=" + dialog.id + " unreadReactions=" + dialog.unread_reactions_count);
+                        }
                         long groupMessagesId = cursor.longValue(20);
                         if (groupMessagesId != 0) {
                             dialogsToLoadGroupMessages.add(new Pair<>(dialogId, groupMessagesId));
@@ -18683,6 +18686,9 @@ public class MessagesStorage extends BaseController {
     }
 
     private void updateUnreadReactionsCountInternal(String tableMentionsForDialogs, String tableMentionsForTopics, String mentionsCounterFieldForDialogs, String mentionsCounterFieldForTopics, long dialogId, long topicId, int count, boolean increment) {
+        if (xie.fa.gram.helpers.chat.ReactionDebugHelper.isEnabled()) {
+            FileLog.d("InuRx db setCount table=" + mentionsCounterFieldForDialogs + " did=" + dialogId + " topic=" + topicId + " count=" + count + " increment=" + increment + " from " + xie.fa.gram.helpers.chat.ReactionDebugHelper.caller());
+        }
         storageQueue.postRunnable(() -> {
             SQLitePreparedStatement state = null;
             if (topicId != 0) {
@@ -18849,6 +18855,9 @@ public class MessagesStorage extends BaseController {
             }
         }
 
+        if (xie.fa.gram.helpers.chat.ReactionDebugHelper.isEnabled()) {
+            FileLog.d("InuRx db markRead table=" + tableMentionsForDialogs + " did=" + dialogId + " topic=" + topicId + " mid=" + messageId + " wasUnread=" + wasUnread + " hasTtl=" + hasTtl + " from " + xie.fa.gram.helpers.chat.ReactionDebugHelper.caller());
+        }
         if (wasUnread) {
             getMessagesController().inu_onMessageUnreadContentRead(dialogId, topicId, hasTtl ? 0 : messageId, updateReactions);
         }
@@ -18863,6 +18872,9 @@ public class MessagesStorage extends BaseController {
     }
 
     private void updateDialogUnreadReactionsInternal(long dialogId, long topicId, int newUnreadCount, boolean increment, boolean isReactions) {
+        if (xie.fa.gram.helpers.chat.ReactionDebugHelper.isEnabled()) {
+            FileLog.d("InuRx db addCount isReactions=" + isReactions + " did=" + dialogId + " topic=" + topicId + " delta=" + newUnreadCount + " increment=" + increment + " from " + xie.fa.gram.helpers.chat.ReactionDebugHelper.caller());
+        }
         final String mentionsCounterFieldForDialogs = isReactions ? "unread_reactions" : "unread_poll_votes";
         final String mentionsCounterFieldForTopics = isReactions ? "unread_reactions" : "unread_poll_votes";
 
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Cells/ChatMessageCell.java b/TMessagesProj/src/main/java/org/telegram/ui/Cells/ChatMessageCell.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Cells/ChatMessageCell.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Cells/ChatMessageCell.java
@@ -7241,6 +7241,8 @@ public class ChatMessageCell extends BaseCell implements SeekBar.SeekBarDelegate
                 } else {
                     reactionsLayoutInBubble.setMessage(null, false, false, resourcesProvider);
                 }
+            } else {
+                xie.fa.gram.helpers.chat.ReactionDebugHelper.onReactionsLayoutKept(this, messageObject);
             }
 
 
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
@@ -8929,6 +8929,9 @@ public class ChatActivity extends BaseFragment implements
                 updatePollVotesMentionButton(false);
             }
         }
+        if (xie.fa.gram.helpers.chat.ReactionDebugHelper.isEnabled()) {
+            FileLog.d("InuRx[" + classGuid + "] init did=" + dialog_id + " topic=" + getTopicId() + " reactions=" + reactionsMentionCount + " pollVotes=" + pollVotesMentionCount);
+        }
 
         if (getDialogId() == getUserConfig().getClientUserId() && chatMode != MODE_SAVED) {
             savedMessagesHint = new HintView2(context, HintView2.DIRECTION_TOP);
@@ -16244,6 +16247,9 @@ public class ChatActivity extends BaseFragment implements
                     updatePollVotesMentionButton(true);
                 }
                 if (messageObject != null && fragmentOpened && openAnimationEnded && !inPreviewMode && (chatListItemAnimator == null || !chatListItemAnimator.isRunning()) && messageCell.checkUnreadReactions(clipTop, chatListView.getMeasuredHeight() - blurredViewBottomOffset)) {
+                    if (xie.fa.gram.helpers.chat.ReactionDebugHelper.isEnabled()) {
+                        FileLog.d("InuRx[" + classGuid + "] visible read mid=" + messageObject.getId() + " count=" + reactionsMentionCount);
+                    }
                     reactionsMentionCount--;
                     getMessagesStorage().markMessageReactionsAsRead(getDialogId(), getTopicId(), messageObject.getId());
                     if (reactionsMentionCount <= 0) {
@@ -16263,6 +16269,7 @@ public class ChatActivity extends BaseFragment implements
                     }
                     updateReactionsMentionButton(true);
                 }
+                xie.fa.gram.helpers.chat.ReactionDebugHelper.checkStaleUnread(messageCell, reactionsMentionCount);
                 getDownloadController().checkUnviewedDownloads(messageCell.getId(), dialog_id);
                 boolean allowPlayEffect = messageObject.getEffect() != null || ((messageObject.messageOwner.media != null && !messageObject.messageOwner.media.nopremium) || (messageObject.isAnimatedEmojiStickerSingle() && dialog_id > 0));
                 if ((chatListItemAnimator == null || !chatListItemAnimator.isRunning()) && (!messageObject.isOutOwner() || messageObject.forcePlayEffect) && allowPlayEffect && !messageObject.messageOwner.premiumEffectWasPlayed && (messageObject.isPremiumSticker() || messageCell.getEffect() != null || messageObject.isAnimatedEmojiStickerSingle()) && emojiAnimationsOverlay.isIdle() && emojiAnimationsOverlay.checkPosition(messageCell, chatListViewPaddingTop, chatListView.getMeasuredHeight() - blurredViewBottomOffset)) {
@@ -24416,6 +24423,9 @@ public class ChatActivity extends BaseFragment implements
             long topicId = (long) args[1];
             if (dialogId == dialog_id && ((!isTopic && (!ChatObject.isMonoForum(currentChat) || getTopicId() == 0)) || topicId == getTopicId())) {
                 reactionsMentionCount = (int) args[2];
+                if (xie.fa.gram.helpers.chat.ReactionDebugHelper.isEnabled()) {
+                    FileLog.d("InuRx[" + classGuid + "] counter notification did=" + dialogId + " topic=" + topicId + " count=" + reactionsMentionCount + " messages=" + args[3]);
+                }
                 ArrayList<Integer> messages = null;
                 if (args[2] != null) {
                     messages = (ArrayList<Integer>) args[3];
@@ -24424,6 +24434,9 @@ public class ChatActivity extends BaseFragment implements
                     for (int i = 0; i < messages.size(); i++) {
                         int messageId = messages.get(i);
                         BaseCell cell = findMessageCell(messageId, true);
+                        if (xie.fa.gram.helpers.chat.ReactionDebugHelper.isEnabled()) {
+                            FileLog.d("InuRx[" + classGuid + "] counter notification mid=" + messageId + " visibleCell=" + (cell != null) + " count=" + reactionsMentionCount);
+                        }
                         if (cell != null && reactionsMentionCount > 0) {
                             reactionsMentionCount--;
                             getMessagesStorage().markMessageReactionsAsRead(getDialogId(), getTopicId(), messageId);
@@ -27036,6 +27049,7 @@ public class ChatActivity extends BaseFragment implements
                 }
                 if (messageObject.type >= 0) {
                     messageObject.reactionsChanged |= old.isBotPendingDraft && !messageObject.isBotPendingDraft;
+                    xie.fa.gram.helpers.chat.ReactionDebugHelper.onMessageObjectReplaced(old, messageObject);
                     messageObject.copyStableParams(old);
                     messages.set(index, messageObject);
                     if (chatAdapter != null && !chatAdapter.isFiltered) {
@@ -46960,7 +46974,13 @@ public class ChatActivity extends BaseFragment implements
             loadLastUnreadMention();
         } else if (buttonId == ChatActivitySideControlsButtonsLayout.BUTTON_REACTIONS) {
             wasManualScroll = true;
+            if (xie.fa.gram.helpers.chat.ReactionDebugHelper.isEnabled()) {
+                FileLog.d("InuRx[" + classGuid + "] jump click count=" + reactionsMentionCount);
+            }
             getMessagesController().getNextReactionMention(dialog_id, getTopicId(), reactionsMentionCount, (messageId) -> {
+                if (xie.fa.gram.helpers.chat.ReactionDebugHelper.isEnabled()) {
+                    FileLog.d("InuRx[" + classGuid + "] jump result mid=" + messageId + " count=" + reactionsMentionCount);
+                }
                 if (messageId == 0) {
                     reactionsMentionCount = 0;
                     updateReactionsMentionButton(true);
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Components/Reactions/ReactionsLayoutInBubble.java b/TMessagesProj/src/main/java/org/telegram/ui/Components/Reactions/ReactionsLayoutInBubble.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Components/Reactions/ReactionsLayoutInBubble.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Components/Reactions/ReactionsLayoutInBubble.java
@@ -112,7 +112,7 @@ public class ReactionsLayoutInBubble {
     HashMap<String, ReactionButton> lastDrawingReactionButtons = new HashMap<>();
     HashMap<String, ReactionButton> lastDrawingReactionButtonsTmp = new HashMap<>();
     View parentView;
-    MessageObject messageObject;
+    public MessageObject messageObject;
     Theme.ResourcesProvider resourcesProvider;
     private Integer scrimViewReaction;
     private float scrimProgress;
